# Copy this file to .env and fill in your values.
# docker compose reads .env automatically.
#
# DB_HOST is set to the 'postgres' container inside docker-compose.yml
# and does not need to be set here unless you use an external database.

DB_USER=cgy_user
DB_PASSWORD=change_me
DB_NAME=cgyinfo
